import { Button } from "@mui/material";

export const MRButton = () => {
  return (
    <Button
      variant="contained"
      sx={{
        borderRadius: "100px",
      }}
    >
      Текст
    </Button>
  );
};
